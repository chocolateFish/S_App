package sokoban.game.gameModel;

/**
 * Created by User on 19/05/2015.
 */
/*public enum PositionTypes {
    FLOOR(BlockTypes.FlOOR, BlockTypes.BLANK),
    WAlL(BlockTypes.WALL, BlockTypes.BLANK),
    TARGET(BlockTypes.TARGET, BlockTypes.BLANK),
    EMPTY(BlockTypes.EMPTY, BlockTypes.BLANK),
    MAN_ON_FLOOR(BlockTypes.FlOOR, BlockTypes.MAN),
    MAN_ON_TARGET(BlockTypes.TARGET, BlockTypes.MAN),
    BOX_ON_FLOOR(BlockTypes.FlOOR, BlockTypes.BOX),
    BOX_ON_TARGET(BlockTypes.TARGET, BlockTypes.BOX);

    private BlockTypes myFixed;
    private BlockTypes myMovable;

    private PositionTypes(BlockTypes fixed, BlockTypes movable) {
        this.myFixed = fixed;
        this.myMovable = movable;
    }


    public BlockTypes getFixed(){
       return this.myFixed;
    }

    public BlockTypes getMovable(){
        return this.myMovable;
    }

}
*/
